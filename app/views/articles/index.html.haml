- @articles.each do |a|
  %h2= link_to a.title, a #To link to the individual articles page you dont need write out a path ex: 'articles_path'
  %p
    Published at
    = a.created_at.strftime('%b %d,%y')
  %p= truncate(a.content, length: 200)

